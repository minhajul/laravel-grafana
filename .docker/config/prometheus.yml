global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'laravel'
    metrics_path: '/prometheus'  # Spatie uses /prometheus, not /metrics
    static_configs:
      - targets: ['laravel.test:8000']
        # Ensure you have a metrics exporter package installed in Laravel
        # or change port to 9090 if using a sidecar exporter.

  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100']

  - job_name: 'mysql-exporter'
    static_configs:
      - targets: ['mysql-exporter:9104']

  - job_name: 'blackbox'
    metrics_path: /probe
    params:
      module: [http_2xx]
    static_configs:
      - targets:
          - http://laravel.test:8000/
    relabel_configs:
      - source_labels: [__address__]
        target_label: __param_target
      - source_labels: [__param_target]
        target_label: instance
      - target_label: __address__
        replacement: blackbox-exporter:9115
